# Copyright 2022 Canonical Ltd.
# See LICENSE file for licensing details.

type: "charm"
bases:
  - build-on:
    - name: "ubuntu"
      channel: "20.04"
    run-on:
    - name: "ubuntu"
      channel: "20.04"

parts:
  charm:
    build-packages:
      # NOTE(claudiub): required for installing psycopg2 (postgresql-related)
      - gcc
      - python3-dev
      - libpq-dev
